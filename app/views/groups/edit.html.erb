Edit Your Group

<%= form_for :group, url: groups_path do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :description %>
  <%= f.text_field :description %><br>

<%= f.submit :submit %>

<% end %>
