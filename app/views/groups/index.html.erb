<div class='container'>
	You are a member of <%= pluralize(@groups.count, 'group') %>.<br><br>

		<% @groups.each do |g| %>
			Group Name: <%= link_to g.name, group_path(g.id) %><br>
			Description: <%= g.description %><br><br>
			<!-- <ul>
				<li>< %= link_to "Create Question", new_group_question_path(g.id) %></li>
				<li>< %= link_to "Vote", new_group_vote_path(g.id) %></li>
				<li>< %= link_to "View Vote Results", group_votes_path(g.id) %></li>
			</ul> -->
		<% end %>
</div>